import styled from "styled-components";

export const Container = styled.div`
  padding: 20px 0;
  background-color: var(--pure-white);
  box-shadow: 1px 4px 4px -1px rgba(150, 150, 150, 1);
  border-radius: 5px;
`;
